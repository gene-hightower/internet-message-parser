"use strict";

const assert = require("assert");

const { parse } = require("../lib/index.js");

describe("try anything", function () {
    it("", function () {
        console.log(JSON.stringify(parse('atom', 'foo')));
    });
});
