import { Message, is_structured_header } from "../lib/Message";

describe("a test", () => {
  it("this should pass", () => {
    const pass = "pass";
    expect(pass).toStrictEqual("pass");
  });
});
